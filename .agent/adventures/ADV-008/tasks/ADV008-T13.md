---
id: ADV008-T13
title: OBJ writer + semantic propagation
stage: completed
status: passed
created: 2026-04-14T12:20:00Z
updated: 2026-04-14T12:54:00Z
iterations: 1
assignee: geometry-engineer
files: [shape_grammar/tools/obj_writer.py, shape_grammar/tools/semantic.py]
repos: []
depends_on: [ADV008-T12]
tags: [runtime, semantic, phase-d]
adventure_id: ADV-008
adventure_plan: plan-phase-d-runtime
target_conditions: [TC-07, TC-08]
evaluation:
  access_requirements: [Read, Write, Bash]
  skill_set: [Python, OBJ format]
  estimated_duration: 25min
  estimated_tokens: 45000
---

## Description
Implement `shape_grammar/tools/obj_writer.py` (write OBJ with one group per semantic label) and `shape_grammar/tools/semantic.py` (propagate labels through IR per inheritance rule). Per `design-semantic-rendering.md`.

## Acceptance Criteria
- [x] `write_obj(terminals, '/tmp/x.obj')` produces a non-empty file with `g` directives for each distinct label.
- [x] `propagate(ir)` returns an IR where every rule has a non-default label (inherited if not declared).

## Design
See `.agent/adventures/ADV-008/designs/design-semantic-rendering.md`.
Plan reference: `.agent/adventures/ADV-008/plans/plan-phase-d-runtime.md`

## Log
- [2026-04-14T12:20:00Z] created: Task created from adventure ADV-008
- [2026-04-14T12:54:00Z] implementer: Created obj_writer.py (write_obj with unit-cube geometry, group-per-label, binary-safe text output) and semantic.py (propagate via lhs-chain parent traversal with fallback to rule.id). Both ACs verified by inline smoke tests. TC-10 pristine — diff matches baseline exactly. All 38 shape_grammar tests pass.
- [2026-04-14T15:36:00Z] adventure-task-reviewer: FAILED — obj_writer.py and semantic.py are both correct in isolation (all 9 test_semantic.py tests pass, write_obj produces proper OBJ with g directives). However, evaluator.py CLI (line 416) still calls _write_stub_obj() instead of write_obj, so the end-to-end TC-07 proof command produces a stub-only OBJ with no geometry or g directives. Fix: wire evaluator.py to import and call write_obj from obj_writer.py.
- [2026-04-14T15:39:20Z] adventure-task-reviewer: PASSED — fix confirmed. evaluator.py now imports write_obj lazily inside _cli_main (avoids circular dep). _write_stub_obj removed. TC-07 exits 0 with proper real-write_obj header (# shape_grammar OBJ output / # seed: 42 / # terminals: 0). TC-08: 9/9 test_semantic.py pass. Full suite: 79/79 pass. Both ACs met. 0 issues.
