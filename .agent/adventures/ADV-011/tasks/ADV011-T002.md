---
id: ADV011-T002
title: Design test strategy for Ark Core Unification
stage: completed
status: passed
created: 2026-04-15T02:55:00Z
updated: 2026-04-15T03:15:00Z
iterations: 1
assignee: researcher
files:
  - .agent/adventures/ADV-011/tests/test-strategy.md
repos: []
depends_on: []
tags: [research, core, unification, testing]
adventure_id: ADV-011
adventure_plan: plan-wave-a-harvest
target_conditions: [TC-TS-1]
evaluation:
  access_requirements: [Read designs/, Read schemas/, Write tests/]
  skill_set: [test-to-requirement mapping, shell + stdlib unittest]
  estimated_duration: 15min
  estimated_tokens: 22000
---

## Description
Design automated tests covering every target condition with `proof_method: autotest`. Produce `tests/test-strategy.md` with the TC → proof-command mapping. Specify the `run-all.sh` CI aggregator structure and the two `unittest` files (`test_coverage_arithmetic.py`, `test_mapping_completeness.py`). For each `poc` TC, document the justification inline.

## Acceptance Criteria
- File exists.
- Every TC in the manifest is listed with its proof-command string.
- Every `poc` TC has a justification sentence.

## Design

### Approach

Produce a single authoritative mapping document at
`.agent/adventures/ADV-011/tests/test-strategy.md` that (a) lists **every** TC
from the manifest (TC-001..TC-021 + TC-TS-1 = 22 rows), (b) copies the exact
proof-command string from the manifest's `Proof Command` column, (c) marks
each row `autotest` or `poc`, (d) attaches a justification sentence to every
`poc` row, and (e) includes the CI aggregator skeleton and two
`unittest`-file skeletons as fenced code blocks so the T011 implementer has
zero design ambiguity.

This task is **design-only** — it does not create the `.sh` / `.py` files;
those are produced by T011. T002 and T001 run in parallel because the
test-strategy needs only the manifest + designs, not the harvest output.

### Target Files (this task writes)

- `.agent/adventures/ADV-011/tests/test-strategy.md` — NEW. The TC →
  proof-command mapping, `run-all.sh` skeleton, and two unittest-file
  skeletons. Produced by this task.

### Target Files (later tasks, specified by this design)

- `.agent/adventures/ADV-011/tests/run-all.sh` — NEW, produced by T011 from
  the skeleton below.
- `.agent/adventures/ADV-011/tests/test_coverage_arithmetic.py` — NEW,
  produced by T011 from the skeleton below.
- `.agent/adventures/ADV-011/tests/test_mapping_completeness.py` — NEW,
  produced by T011 from the skeleton below.

### Structure of `tests/test-strategy.md`

```
# Test Strategy — ADV-011 Ark Core Unification

## Scope
  Short paragraph: research adventure, autotest-first, `manual` disallowed,
  single CI entrypoint is run-all.sh, python unittest for argumentative
  checks.

## Proof Method Policy
  - autotest: shell + grep + test -f + python -m unittest
  - poc: allowed only with justification; see ## POC Justifications
  - manual: DISALLOWED — any manual proof is a design bug

## TC → Proof Command Mapping
  Master table, 22 rows, columns:
  | TC | Method | Proof Command | Produced By Task |

## POC Justifications
  One subsection per poc TC (currently TC-004).

## CI Aggregator (run-all.sh)
  Fenced bash code block with the skeleton (below).

## Python Unittest Files
  Two fenced code blocks with the skeletons (below).

## Invariants
  - Every TC in the manifest appears exactly once in the master table.
  - Every proof command is copied verbatim from the manifest.
  - `grep -cE "^\| TC-" tests/test-strategy.md` ≥ 22 (one per TC row).
```

### Master TC → Proof-Command Mapping Template

The implementer MUST copy each `Proof Command` cell from
`.agent/adventures/ADV-011/manifest.md` verbatim (preserving all pipes,
quotes, and escapes). Rows and methods:

| TC       | Method    | Produced By | Notes |
|----------|-----------|-------------|-------|
| TC-001   | autotest  | T001        | existence + per-adventure grep |
| TC-002   | autotest  | T001        | column-header regex |
| TC-TS-1  | autotest  | T002 (self) | `test -f tests/test-strategy.md` + `grep -cE "^\| TC-" ≥ 20` |
| TC-003   | autotest  | T003        | existence + `## Per-Bucket Rationale` |
| TC-004   | poc       | T003 + T011 | bucket allowlist enforced by `test_mapping_completeness.py::test_buckets_allowlist` |
| TC-005   | autotest  | T004        | six seed keywords present |
| TC-006   | autotest  | T004        | awk column-count check |
| TC-007   | autotest  | T005        | ≥ 9 table rows |
| TC-008   | autotest  | T005        | disposition regex |
| TC-009   | autotest  | T006        | one verdict row per stdlib island |
| TC-010   | autotest  | T006        | ADV-008 citation |
| TC-011   | autotest  | T007        | four shared pass names |
| TC-012   | autotest  | T007        | every verify/codegen module named |
| TC-013   | autotest  | T008        | ADV-010 citation |
| TC-014   | autotest  | T008        | seven subsystem names |
| TC-015   | autotest  | T009        | no empty verdict cells |
| TC-016   | autotest  | T009 + T011 | `python -m unittest` on `test_coverage_arithmetic` |
| TC-017   | autotest  | T010        | 3 ≤ count ≤ 6 |
| TC-018   | autotest  | T010        | serial ordering regex |
| TC-019   | autotest  | T011        | `bash run-all.sh` exits 0 |
| TC-020   | autotest  | T011        | `python -m unittest discover` exits 0 |
| TC-021   | autotest  | T012        | nine artefact keywords |

Canonical row format in `test-strategy.md` (exact column header is required
by TC-TS-1's `grep -cE "^\| TC-"` check):

```
| TC | Method | Produced By | Proof Command |
|----|--------|-------------|---------------|
| TC-001 | autotest | T001 | `test -f .agent/adventures/ADV-011/research/concept-inventory.md && for a in 001 002 003 004 005 006 007 008 010; do grep -q "ADV-$a" .agent/adventures/ADV-011/research/concept-inventory.md \|\| exit 1; done` |
| TC-002 | autotest | T001 | `grep -E "^\| concept \| source_adventure \| source_artefact \| description \|" .agent/adventures/ADV-011/research/concept-inventory.md` |
| TC-TS-1 | autotest | T002 | `test -f .agent/adventures/ADV-011/tests/test-strategy.md && [ $(grep -cE "^\| TC-" .agent/adventures/ADV-011/tests/test-strategy.md) -ge 20 ]` |
... (19 more rows — one per remaining manifest TC, verbatim) ...
```

### POC Justification Template (TC-004)

One section per `poc` TC. Current list: TC-004 only.

```
### TC-004 — bucket allowlist
Method: poc
Justification: The check "every row's bucket ∈ {descriptor, builder,
controller, out-of-scope}" is argumentative over table cells. A pure
shell/grep proof would require parsing markdown pipe-tables, which is
fragile. Instead a python unittest (`test_buckets_allowlist` in
`test_mapping_completeness.py`) parses the file and asserts the allowlist.
This is `poc` because the proof command points at a named python test
function, not a one-liner shell command.
Proof Command: `python -m unittest .agent.adventures.ADV-011.tests.test_mapping_completeness.TestMappingCompleteness.test_buckets_allowlist`
```

### `run-all.sh` Skeleton (fenced in test-strategy.md)

```bash
#!/usr/bin/env bash
# run-all.sh — ADV-011 CI aggregator.
# Produced by T011 from the skeleton in test-strategy.md.
# Runs every TC proof command in manifest order. Exit 0 only if all pass.
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/../../../.." && pwd)"
cd "$ROOT"

FAIL=0
run_tc() {
  local id="$1"; shift
  echo "==> $id"
  if bash -c "$*"; then
    echo "    PASS $id"
  else
    echo "    FAIL $id"
    FAIL=1
  fi
}

# --- TC-001 ----------------------------------------------------------------
run_tc TC-001 'test -f .agent/adventures/ADV-011/research/concept-inventory.md && for a in 001 002 003 004 005 006 007 008 010; do grep -q "ADV-$a" .agent/adventures/ADV-011/research/concept-inventory.md || exit 1; done'

# --- TC-002 ----------------------------------------------------------------
run_tc TC-002 'grep -E "^\| concept \| source_adventure \| source_artefact \| description \|" .agent/adventures/ADV-011/research/concept-inventory.md'

# --- TC-TS-1 ---------------------------------------------------------------
run_tc TC-TS-1 'test -f .agent/adventures/ADV-011/tests/test-strategy.md && [ $(grep -cE "^\| TC-" .agent/adventures/ADV-011/tests/test-strategy.md) -ge 20 ]'

# --- TC-003 .. TC-021 ------------------------------------------------------
# ... one run_tc line per manifest row, copied verbatim ...

# --- Python unittests -------------------------------------------------------
run_tc TC-016 'python -m unittest discover -s .agent/adventures/ADV-011/tests -p "test_coverage_arithmetic.py" -v'
run_tc TC-020 'python -m unittest discover -s .agent/adventures/ADV-011/tests -v'

exit "$FAIL"
```

Skeleton rules the implementer must follow:
1. Use `set -euo pipefail` but wrap each TC in `run_tc` so one failure does
   not short-circuit the rest — collect `FAIL=1` and exit at the end.
2. Proof commands are copied **verbatim** from the manifest (and therefore
   from `test-strategy.md`). No editorial rewriting.
3. Order follows the manifest TC numeric order, with `TC-TS-1` placed
   between `TC-002` and `TC-003` (alphabetic in manifest).
4. The script must be runnable from any cwd (resolves `$ROOT` relative to
   its own location).

### `test_coverage_arithmetic.py` Skeleton

Validates TC-016: in `research/validation-coverage.md`, the row-level verdict
columns must satisfy `covered + retired + deferred = total_source_TCs`.

```python
"""Validates TC-016 — validation-coverage arithmetic.

The validation-coverage.md file produced by T009 lists every source TC from
ADV-001..008 with a verdict column whose value is one of
COVERED-BY / RETIRED-BY / DEFERRED-TO. This unittest parses the file and
asserts the count identity.
"""

import pathlib
import re
import unittest

COVERAGE = pathlib.Path(
    ".agent/adventures/ADV-011/research/validation-coverage.md"
)

VERDICTS = ("COVERED-BY", "RETIRED-BY", "DEFERRED-TO")


class TestCoverageArithmetic(unittest.TestCase):
    """TC-016: covered + retired + deferred == total_source_TCs."""

    def setUp(self):
        self.assertTrue(
            COVERAGE.exists(),
            f"Missing {COVERAGE} — T009 must produce it before T011 runs.",
        )
        self.text = COVERAGE.read_text(encoding="utf-8")

    def _count_rows_by_verdict(self):
        counts = {v: 0 for v in VERDICTS}
        total = 0
        for line in self.text.splitlines():
            if not line.startswith("| TC-"):
                continue
            total += 1
            for v in VERDICTS:
                if v in line:
                    counts[v] += 1
                    break
        return total, counts

    def test_each_tc_has_exactly_one_verdict(self):
        """Every TC row must match exactly one of the three verdicts."""
        total, counts = self._count_rows_by_verdict()
        self.assertGreater(total, 0, "No TC rows found in validation-coverage.md")
        self.assertEqual(
            sum(counts.values()),
            total,
            f"Some rows have 0 or >1 verdicts. counts={counts}, total={total}",
        )

    def test_arithmetic_holds(self):
        """TC-016: covered + retired + deferred == total."""
        total, counts = self._count_rows_by_verdict()
        self.assertEqual(
            counts["COVERED-BY"] + counts["RETIRED-BY"] + counts["DEFERRED-TO"],
            total,
        )


if __name__ == "__main__":
    unittest.main()
```

### `test_mapping_completeness.py` Skeleton

Validates TC-004 (bucket allowlist, `poc`) and the cross-file completeness
invariant that every concept listed in `concept-inventory.md` is referenced
in `concept-mapping.md`.

```python
"""Validates TC-004 and cross-file completeness.

TC-004 (poc): every bucket value in concept-mapping.md is one of
  {descriptor, builder, controller, out-of-scope}.
Cross-file: every concept named in concept-inventory.md appears at least
  once in concept-mapping.md (no dropped concepts during classification).
"""

import pathlib
import re
import unittest

INVENTORY = pathlib.Path(
    ".agent/adventures/ADV-011/research/concept-inventory.md"
)
MAPPING = pathlib.Path(
    ".agent/adventures/ADV-011/research/concept-mapping.md"
)

ALLOWED_BUCKETS = {"descriptor", "builder", "controller", "out-of-scope"}


def _table_rows(text):
    """Yield cell-list for each pipe-table data row (skips header + ---)."""
    for line in text.splitlines():
        if not line.startswith("|"):
            continue
        if re.match(r"^\|[\s-]+\|", line):
            continue
        cells = [c.strip() for c in line.strip("|").split("|")]
        if cells and cells[0].lower() in {"concept", "tc"}:
            continue  # header row
        yield cells


class TestMappingCompleteness(unittest.TestCase):
    def setUp(self):
        for f in (INVENTORY, MAPPING):
            self.assertTrue(
                f.exists(),
                f"Missing {f} — upstream task must produce it first.",
            )
        self.inventory_text = INVENTORY.read_text(encoding="utf-8")
        self.mapping_text = MAPPING.read_text(encoding="utf-8")

    def test_buckets_allowlist(self):
        """TC-004: every mapping row's bucket column is in the allowlist."""
        # Bucket column index must match schemas/entities.md#ConceptMappingRow.
        # Expected columns: concept | bucket | source_adventure | rationale
        bucket_idx = 1
        for cells in _table_rows(self.mapping_text):
            if len(cells) <= bucket_idx:
                continue
            bucket = cells[bucket_idx].lower()
            self.assertIn(
                bucket,
                ALLOWED_BUCKETS,
                f"Row has invalid bucket '{bucket}': {cells}",
            )

    def test_every_inventory_concept_is_mapped(self):
        """Cross-file: no concept from inventory is silently dropped."""
        inv_concepts = set()
        for cells in _table_rows(self.inventory_text):
            if cells:
                inv_concepts.add(cells[0].lower())
        missing = [
            c for c in inv_concepts if c and c.lower() not in self.mapping_text.lower()
        ]
        self.assertEqual(
            missing,
            [],
            f"Concepts in inventory missing from mapping: {missing[:5]} ...",
        )


if __name__ == "__main__":
    unittest.main()
```

### Implementation Steps (for the T002 implementer, role `researcher`)

1. Read `.agent/adventures/ADV-011/manifest.md` and extract the 22 TC rows
   (TC-001..TC-021 + TC-TS-1). For each, record `id`, `proof_method`, and
   the exact `Proof Command` cell (preserve all `\|` escapes).
2. Create `.agent/adventures/ADV-011/tests/` directory (only the
   `test-strategy.md` file — no `.sh` or `.py` yet; those are T011's job).
3. Write `test-strategy.md` using the structure section above. Populate the
   master mapping table with all 22 rows. Verify
   `grep -cE "^\| TC-" tests/test-strategy.md` returns ≥ 22.
4. Paste the three fenced code blocks (`run-all.sh`,
   `test_coverage_arithmetic.py`, `test_mapping_completeness.py`) verbatim
   from this design into their respective sections of `test-strategy.md`.
5. Add the POC Justifications section with the TC-004 entry.
6. Sanity-check: confirm every `poc` TC in the manifest (currently only
   TC-004) has a justification block.

### Testing Strategy

- Self-test TC-TS-1: after writing, run
  `test -f .agent/adventures/ADV-011/tests/test-strategy.md && [ $(grep -cE "^\| TC-" .agent/adventures/ADV-011/tests/test-strategy.md) -ge 20 ]`
  — must exit 0.
- Visual audit: diff the proof-command column of the strategy's master
  table against the manifest's `Proof Command` column — they must be
  character-identical.
- POC coverage: `grep -c "^### TC-" test-strategy.md` (within the POC
  Justifications section) must equal the count of `poc` rows in the master
  table.

### Risks

- **Proof-command drift** — if a later planner edit changes a manifest
  proof command but forgets to update `test-strategy.md`, the run-all.sh
  produced by T011 will diverge. Mitigation: call out the verbatim-copy
  requirement explicitly, and T011 will rebuild run-all.sh from the
  strategy, not the manifest, so drift is caught at T011 time.
- **Unittest path assumptions** — the skeletons assume cwd is the repo
  root. run-all.sh already `cd`s to `$ROOT` before invoking python, so
  this holds.
- **Pipe-table parser fragility** in `test_mapping_completeness.py` — the
  parser assumes ConceptMappingRow column ordering per
  `schemas/entities.md`. If T003 reorders columns, the `bucket_idx = 1`
  constant must move. Mitigation: docstring references the schema file so
  the dependency is explicit.
- **Parallel-with-T001 constraint** — this task reads only manifest +
  designs + schemas + plans. It does **not** read any research/ output.
  Verified: no reference to `concept-inventory.md` content in the
  strategy — only to its path (which is stable).

## Log
- [2026-04-15T02:55:00Z] created: Task created from adventure ADV-011
- [2026-04-15T03:03:00Z] planner: filled Design section with test-strategy.md structure, TC→proof-command mapping template (22 rows), run-all.sh skeleton, and two unittest skeletons (test_coverage_arithmetic.py, test_mapping_completeness.py); assignee confirmed as researcher; ran parallel to T001 (no harvest dependency); stage: planning -> ready
- [2026-04-15T03:15:00Z] implementer: produced tests/test-strategy.md with 22-row TC→proof-command mapping table (TC-001..TC-021 + TC-TS-1), POC justification for TC-004, run-all.sh skeleton, test_coverage_arithmetic.py skeleton, test_mapping_completeness.py skeleton, and Invariants section; grep -cE "^\| TC-" returns 22 (≥ 20); TC-TS-1 satisfied; stage: ready -> completed
- [2026-04-15T10:00:00Z] adventure-task-reviewer: PASSED — all 3 ACs met, TC-TS-1 verified (22 TC rows >= 20), 1 low-severity issue (duplicate run_tc calls for TC-016/TC-020 in run-all.sh skeleton, cosmetic only, T011 implementer should deduplicate)
