---
id: ADV009-T001
title: Design test strategy for ADV-009 console v2
stage: completed
status: passed
created: 2026-04-14T22:10:00Z
updated: 2026-04-16T00:05:00Z
iterations: 1
assignee: implementer
files: [.agent/adventures/ADV-009/tests/test-strategy.md]
repos: []
depends_on: []
tags: [console, ui, v2, test-design, wave-a]
adventure_id: ADV-009
adventure_plan: plan-audit-and-backend
target_conditions: [TC-034, TC-035]
evaluation:
  access_requirements: [Read, Write]
  skill_set: [test planning, markdown authoring]
  estimated_duration: 12 min
  estimated_tokens: 18000
---

## Description
Author `.agent/adventures/ADV-009/tests/test-strategy.md` mapping every TC with `proof_method: autotest` to a named test function in a named test file (see design-test-strategy.md). Declare the three stdlib-unittest files (`test_server.py`, `test_ui_smoke.py`, `test_ui_layout.py`) and the full run command. Include the `data-testid` contract list as an appendix.

## Acceptance Criteria
- [ ] Every `autotest` TC appears at least once in the strategy's mapping table.
- [ ] Test framework choice (stdlib `unittest`), three test files, and run command are explicit.
- [ ] The `data-testid` appendix lists every hook id used by the designs.

## Design

Summary: produce the single deliverable `.agent/adventures/ADV-009/tests/test-strategy.md`. No Python is written; no `adventures/ADV-009/designs/` file is modified. The doc is a contract that later tasks (T003, T004, T012, T016, T018, T021) must satisfy. See `.agent/designs/ADV009-T001-design.md` for the full plan; the key points below are load-bearing for the implementer.

**Sections to author in `test-strategy.md`**

1. `# Test Strategy — ADV-009 Console v2` title.
2. `## Framework` — stdlib `unittest`, Python 3, zero third-party imports.
3. `## Files` — declare the three core files required by the acceptance criterion:
   - `test_server.py` (Tier 1, backend, in-process)
   - `test_ui_smoke.py` (Tier 2/3, interaction; Playwright is optional-and-skipped if unimportable)
   - `test_ui_layout.py` (Tier 2, static HTML structure via `html.parser`)
   - Plus addendum-extension files: `test_ir.py`, `test_graph_endpoint.py`, `test_pipeline_tab.py`. Label these "addendum extensions"; they still participate in discover.
4. `## Run Command` (exact, fenced):
   ```
   python -m unittest discover -s .agent/adventures/ADV-009/tests -p "test_*.py"
   ```
5. `## TC → Test Function Mapping` — one Markdown table covering every autotest TC from `manifest.md`. Columns: `TC | Test Class.Method | File`. Rows drawn from manifest (source of truth). Required entries:

   test_server.py: TC-025 `TestKnowledgePayload`; TC-026 `TestSummary`; TC-027 `TestDocumentsEndpoint.test_types`; TC-028 `TestDocumentsEndpoint.test_waves`; TC-029 `TestNextAction.test_review`; TC-030 `TestStdlibOnly`; TC-034 `TestStrategyDoc.test_tc_mapping`; TC-035 `TestStrategyDoc.test_framework`.

   test_ui_layout.py: TC-001 `TestAuditPresence`; TC-002 `TestAuditVerdicts`; TC-003 `TestAuditCoverage`; TC-004 `TestTabBar.test_four_tabs`; TC-005 `TestTabBar.test_no_legacy_tabs`; TC-006 `TestHeader`; TC-008 `TestTasksTab.test_buckets`; TC-009 `TestTasksTab.test_card_shape`; TC-013 `TestDocumentsTab.test_chips`; TC-018 `TestOverview.test_progress_bar`; TC-022 `TestDecisions.test_three_cards`; TC-031 `TestVisualSystem.test_classes`; TC-032 `TestVisualSystem.test_no_external`; TC-033 `TestVisualSystem.test_card_usage`.

   test_ui_smoke.py: TC-010 `TestTaskDetail.test_structured`; TC-011 `TestTaskDetail.test_disclosure`; TC-014 `TestDocuments.test_chip_filter`; TC-015 `TestDocuments.test_design_header`; TC-016 `TestDocuments.test_plan_waves`; TC-017 `TestDocuments.test_review_strip`; TC-020 `TestOverview.test_next_action`; TC-024 `TestDecisions.test_state_post`.

   Addendum rows (include in the same table for completeness, labelled with their file): TC-039..TC-045 → `ark/ark.py parse` + `test_ir.py`; TC-046, TC-052..TC-054, TC-058..TC-061 → `test_graph_endpoint.py`; TC-047..TC-051, TC-055, TC-056 → `test_pipeline_tab.py`. TC-036 is the meta-TC asserting the full `discover` command exits 0.

6. `## Tiering Notes` — summarize Tier 1 / Tier 2 / Tier 3 per `design-test-strategy.md §Approach` (cite the design; do not wholesale-copy). Make explicit: Tier 3 (Playwright) is optional and skipped via an import-guard when unavailable; in that case the TC-008/TC-011/TC-013/TC-014/TC-020/TC-024 tests still assert against markup as a Tier-2 fallback so autotest coverage is preserved.
7. `## Appendix A — data-testid Contract` — enumerate every hook id from `design-test-strategy.md §Data-testid hooks`: `tab-overview`, `tab-tasks`, `tab-documents`, `tab-decisions`, `tc-progress-bar`, `tc-progress-label`, `next-action-card`, `next-action-button`, `chip-filter-{all,designs,plans,research,reviews}`, `task-card-{id}`, `task-card-status-{id}`, `doc-list-item-{filename}`, `decisions-card-{state-transitions,permissions,knowledge}`, `show-details-{context}`. Also scan `designs/design-pipeline-graph-view.md` and `designs/design-graph-edit-affordances.md` during authoring and append any further `data-testid` ids they reference (e.g. pipeline tab, graph canvas, node/edge testids). Those two designs are reference-only — do not edit them.

**Authoring order**

1. Read manifest.md TC table (already the source for the mapping rows above).
2. Read design-test-strategy.md for tiering + testid list (reference-only).
3. Skim design-pipeline-graph-view.md + design-graph-edit-affordances.md for any additional testids.
4. Create `.agent/adventures/ADV-009/tests/` directory if absent, write `test-strategy.md` with the seven sections above.
5. Self-verify acceptance criteria before handoff: grep manifest for autotest rows → confirm each row's TC appears in the strategy mapping table; confirm the three core file names appear exactly once each in `## Files`; confirm the run command line is present verbatim; confirm every testid from the designs appears in Appendix A.

**Constraints**

- Do not create any `.py` files — this task is markdown-only.
- Do not modify any `adventures/ADV-009/designs/*.md` file.
- Stay stdlib-only in spec (no Jest, Playwright-required, Pytest, etc. baked into the contract).

Full design: `.agent/designs/ADV009-T001-design.md`.

## Log
- [2026-04-14T22:10:00Z] created: Task created from adventure ADV-009
- [2026-04-15T00:22:00Z] planner: expanded Design with concrete TC-to-test mapping, file list, run command, and testid appendix plan; stage planning -> ready. Full design at .agent/designs/ADV009-T001-design.md.
- [2026-04-16T00:05:00Z] implementer: authored .agent/adventures/ADV-009/tests/test-strategy.md — 7 sections: Framework, Files (3 core + 3 addendum), Run Command, TC→Test mapping (51 autotest TCs), Tiering Notes, Appendix A (22 data-testid hooks). Verified all autotest TCs from manifest appear in mapping table. Stage ready -> completed, status done.
- [2026-04-18T00:00:05Z] adventure-task-reviewer: PASSED — all 3 ACs met, TC-034 and TC-035 both PASS (27/27 test_server.py tests OK). All 52 autotest TCs confirmed in mapping table; three core files and run command explicit; all 24 data-testid hooks present in Appendix A. No issues found.
