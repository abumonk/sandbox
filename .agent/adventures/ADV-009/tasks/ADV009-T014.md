---
id: ADV009-T014
title: Update README for v2
stage: completed
status: passed
created: 2026-04-15T00:05:00Z
updated: 2026-04-16T23:36:26Z
iterations: 0
assignee: planner
files: [.agent/adventure-console/README.md]
repos: []
depends_on: [ADV009-T011]
tags: [console, docs, wave-c]
adventure_id: ADV-009
adventure_plan: plan-testing-and-polish
target_conditions: [TC-038]
evaluation:
  access_requirements: [Read, Edit]
  skill_set: [Markdown authoring]
  estimated_duration: 8 min
  estimated_tokens: 12000
---

## Description
Update `.agent/adventure-console/README.md` to reflect the four-tab v2 layout. Replace the v1 "Main pane - tabs per adventure" table with the Overview/Tasks/Documents/Decisions description. Add a brief note about the `summary` block and the new `/documents` endpoint. Keep the rest (run / how-to / safety) intact.

## Acceptance Criteria
- [ ] README mentions exactly the four v2 tabs (Overview, Tasks, Documents, Decisions) and does not list the nine v1 tabs in the "What it does" section.
- [ ] The endpoints table lists `GET /api/adventures/{id}/documents`.
- [ ] The endpoints table (or a new note) mentions that `GET /api/adventures/{id}` response now includes a `summary` block (tc counts, tasks_by_status, next_action).
- [ ] Run command still "`python .agent/adventure-console/server.py`".
- [ ] Sections "Run", "How the backend works" (header), "Safety", and "Files" remain present and substantively unchanged (wording may be refreshed but structure preserved).
- [ ] No stale references to v1-only tab names ("Designs", "Plans", "Permissions", "Reviews", "Knowledge", "Research", "Log") as top-level tabs.
- [ ] Knowledge-extraction section is updated to note that knowledge curation now lives inside the **Decisions** tab (not a standalone Knowledge tab).

## Design

Documentation-only refresh. No code changes. The README must match the v2 four-tab information architecture established in `design-information-architecture` and the backend additions in `design-backend-endpoints`.

Full design doc: `.agent/designs/ADV009-T014-design.md`.

### Summary

Rewrite two sections of `.agent/adventure-console/README.md`:

1. **"What it does"** — replace the v1 nine-row tab table with a four-row table describing Overview / Tasks / Documents / Decisions. Update the sidebar description to match `design-information-architecture` (state badge + ID + title + one-line subtitle; no task/file counts).
2. **"How the backend works"** — add `GET /api/adventures/{id}/documents` to the endpoints table, and add a short note under the table that `GET /api/adventures/{id}` now includes a derived `summary` block with TC counts, tasks_by_status, and a `next_action` hint.
3. **"Knowledge extraction"** — retarget to note that the Decisions tab surfaces the `## 6. Knowledge Extraction Suggestions` section and that the "Record Selection" control lives there. Extraction behavior (writes `reviews/knowledge-selection.json`) is unchanged.

Keep **Run**, **Safety**, and **Files** sections intact (Files section still lists `server.py`, `index.html`, `README.md`).

### Implementation Steps

1. Read the current `.agent/adventure-console/README.md` to confirm the exact section boundaries before editing.
2. Replace the "Main pane — tabs per adventure" table with the v2 four-tab table:
   - Overview | Concept synopsis, TC progress, next-action card
   - Tasks | Grouped-by-status task cards; click to view full task
   - Documents | Unified browser for designs / plans / research / reviews with type filter chips
   - Decisions | Pending approvals, state-transition controls, permissions, knowledge-suggestion curation
3. Update the "State transitions" paragraph to note those controls now live inside the **Decisions** tab, though the `POST /api/adventures/{id}/state` contract is unchanged.
4. Update the Sidebar paragraph to match the simplified row format (state badge + ID + title + 80-char subtitle; no counts).
5. In the endpoints table under "How the backend works", add a new row:
   `| GET | /api/adventures/{id}/documents | — | Unified list of designs/plans/research/reviews with lightweight metadata |`
6. Immediately below the endpoints table, add a short paragraph (2–3 sentences) describing the `summary` block returned by `GET /api/adventures/{id}` (fields: `tc_total`, `tc_passed`, `tc_failed`, `tc_pending`, `tasks_by_status`, `next_action`). Note it is derived server-side and not persisted.
7. Rewrite the "Knowledge extraction" section so it points to the **Decisions** tab as the surface (was: "Knowledge tab"). Keep the payload description (`reviews/knowledge-selection.json`) and the "console does not mutate knowledge files" disclaimer verbatim.
8. Scan the resulting document for any lingering references to v1 top-level tabs (`Designs`, `Plans`, `Permissions`, `Reviews`, `Knowledge`, `Research`, `Log`) and either remove them or reframe them as sub-concepts of the v2 tabs.
9. Verify the Run command line is still exactly `python .agent/adventure-console/server.py` and the default bind URL is still `http://127.0.0.1:7070`.
10. Leave Safety and Files sections unchanged in content.

### Testing Strategy

Manual (TC-038 is `proof_method: manual`):

- `grep -c` / visual check for the strings "Overview", "Tasks", "Documents", "Decisions" in README.md (all four present).
- `grep` for v1-only top-level tab names to confirm they do not appear as tab rows (they may still appear inside prose that describes Documents sub-types).
- Confirm `/api/adventures/{id}/documents` appears in the endpoints table.
- Confirm the `summary` block is described.
- Confirm Run command unchanged.
- Diff-review: sections "Run", "Safety", "Files" should be unchanged aside from the path-level reality.

### Risks

- Overwriting section headings that other docs link to. Mitigation: keep heading text (`Run`, `What it does`, `How the backend works`, `Knowledge extraction`, `Safety`, `Files`) identical to current values; only edit bodies.
- Drift between README and `design-information-architecture` / `design-backend-endpoints`. Mitigation: mirror the exact tab names and `summary` field names from those designs.
- This task depends on ADV009-T011 (which delivers the four-tab UI); if that task drifts from the information-architecture design the README could go out of sync. Mitigation: source-of-truth for tab names is the design doc, not the implementation at the moment of writing.

## Log
- [2026-04-15T00:05:00Z] created: Task created from adventure ADV-009
- [2026-04-15T00:15:00Z] planner: Expanded Design section with concrete implementation steps, refined acceptance criteria (added explicit `summary` block + Decisions-tab knowledge pointer), wrote full design doc at `.agent/designs/ADV009-T014-design.md`, flipped stage planning→ready.
- [2026-04-16T23:36:26Z] implementer: Rewrote "What it does" section with 4-tab v2 table (Overview/Tasks/Documents/Decisions) and updated sidebar description; added /api/adventures/{id}/documents row to endpoints table; added summary block description below table; changed Knowledge extraction section to reference Decisions tab. 1 file modified.
- [2026-04-18T00:00:05Z] adventure-task-reviewer: All 7 ACs met, TC-038 (manual) verified via grep checks — PASSED. README correctly reflects v2 four-tab layout, /documents endpoint, summary block, and Decisions-tab knowledge curation reference.
