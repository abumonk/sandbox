---
id: ADV010-T002
title: Test strategy document (mandatory test design task)
stage: completed
status: passed
created: 2026-04-15T07:00:00Z
updated: 2026-04-16T00:00:00Z
iterations: 0
assignee: planner
files:
  - .agent/adventures/ADV-010/tests/test-strategy.md
repos: []
depends_on: []
tags: [telemetry, test-design, test-strategy, telemetry-engineer]
adventure_id: ADV-010
adventure_plan: plan-wave-a-measurement-sources
target_conditions: [TC-TS-1, TC-RG-1]
evaluation:
  access_requirements: [Read(designs,schemas), Write(tests/)]
  skill_set: [unittest, test-to-requirement mapping]
  estimated_duration: 15min
  estimated_tokens: 25000
---

## Description
Author `tests/test-strategy.md` mapping every autotest TC to a named `unittest` function in a specific file. Record the CI one-liner. Already drafted; implementer verifies it covers every TC with `Proof Method: autotest` from the final manifest and adds any missing rows.

## Acceptance Criteria
- Every autotest TC from the manifest appears in the TC-to-function mapping table.
- The CI one-liner is present and is a `python -m unittest discover ...` command.
- File lists one test file per design area (8 files expected).

## Design

**Summary**: Verify and amend the pre-drafted `tests/test-strategy.md` to ensure complete TC-to-function coverage across all 35 autotest TCs from the manifest.

**Design doc**: [ADV010-T002-design.md](../designs/ADV010-T002-design.md)

**Key steps**:
1. Extract all 35 autotest TCs from `manifest.md` (every row with `Proof Method: autotest`).
2. Cross-check each TC against the per-file coverage tables in `test-strategy.md` -- confirm each has a test file, function name (`test_<snake_case>`), and assertion description.
3. Verify test file count: 9 files covering 7 design areas + task-actuals split + regression gate (satisfies "8 files expected" criterion as 8+).
4. Confirm CI one-liner: `python -m unittest discover -s .agent/adventures/ADV-010/tests -v`.
5. Confirm fixtures strategy section documents events/, metrics/, manifests/ fixture categories.
6. Confirm self-check clause (test_regression.py grep-counts TCs in both manifest and strategy file).
7. Amend any missing TCs or files. Final TC row count must equal 35.

**Proof**: TC-TS-1 via `test_regression.py` preamble; TC-RG-1 via `test_full_discover_exits_zero`.

## Log
- [2026-04-15T07:00:00Z] created: Task created from adventure ADV-010
- [2026-04-16T00:01:00Z] planner: Expanded design section with 7-step implementation plan covering TC-to-function verification, file count check, CI one-liner, fixtures strategy, self-check clause, and gap amendment. Wrote design doc ADV010-T002-design.md. Set stage to ready.
- [2026-04-18T00:00:00Z] adventure-task-reviewer: PASSED — 35/35 autotest TCs covered, CI one-liner present, 9 test files confirmed, TC-TS-1 and TC-RG-1 both PASS (39 tests, 1 platform skip).
- [2026-04-18T00:00:00Z] implementer: Verified and amended tests/test-strategy.md. Found TC-TS-1 missing from per-file coverage tables and fixtures strategy section absent. Added TC-TS-1 to test_regression.py table (test_strategy_coverage function). Added Fixtures section documenting events/, metrics/, manifests/ fixture categories with representative file names. Final TC row count: 35 unique TC IDs each appearing exactly once, matching all 35 autotest TCs from manifest. 9 test files confirmed (one per design area plus regression gate). CI one-liner and self-check clause both verified present.
