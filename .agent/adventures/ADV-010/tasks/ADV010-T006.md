---
id: ADV010-T006
title: Aggregator module
stage: completed
status: passed
created: 2026-04-15T07:00:00Z
updated: 2026-04-16T00:00:00Z
iterations: 0
assignee: planner
files:
  - .agent/telemetry/aggregator.py
repos: []
depends_on: [ADV010-T005]
tags: [telemetry, aggregator, implementation, telemetry-engineer]
adventure_id: ADV-010
adventure_plan: plan-wave-b-capture-mechanism
target_conditions: [TC-AG-1, TC-AG-2, TC-AG-3, TC-AG-6]
evaluation:
  access_requirements: [Read, Write(.agent/telemetry/)]
  skill_set: [file I/O, atomic renames]
  estimated_duration: 25min
  estimated_tokens: 40000
---

## Description
Implement `.agent/telemetry/aggregator.py`: `parse_rows(metrics_path)`, `recompute_frontmatter(metrics_path)`, `format_duration(seconds)`. Atomic write via `metrics.md.tmp` + `os.replace`.

## Acceptance Criteria
- Given a fixture metrics.md with N rows, frontmatter `total_*` equals the row sum after `recompute_frontmatter()`.
- `format_duration(16*60) == "16min"`, `format_duration(95) == "95s"`, `format_duration(2*3600 + 15*60) == "2h 15min"`.
- Running the recompute twice produces byte-identical file.

## Design
Implement `aggregator.py` with three public functions: `parse_rows()` reads a metrics.md pipe-table into `MetricsRow` instances, `recompute_frontmatter()` derives totals from rows and atomically rewrites the YAML frontmatter via `os.replace()`, and `format_duration()` renders integer seconds as human-friendly strings. Internal helpers handle YAML-subset parsing/serialization without PyYAML. Legacy `~`-prefixed approximate values are stripped during parsing. All errors surface as `AggregationError`.

See: [designs/ADV010-T006-design.md](../designs/ADV010-T006-design.md)

## Log
- [2026-04-15T07:00:00Z] created: Task created from adventure ADV-010
- [2026-04-16T00:00:00Z] planner: Designed aggregator module — 1 target file, 6 implementation steps, 4 acceptance criteria mapped to TCs. Design at designs/ADV010-T006-design.md
- [2026-04-18T12:35:20Z] implementer: implemented aggregator.py — parse_rows (tilde-strip, AggregationError on parse failure), recompute_frontmatter (atomic os.replace via .md.tmp), format_duration (threshold 120s per TC); all 3 ACs pass (AC1 totals, AC2 idempotent, AC3 format_duration)
- [2026-04-18T00:02:00Z] adventure-task-reviewer: PASSED — build N/A, 4/4 tests pass, 3/3 ACs met, 4/4 TCs pass. Minor doc discrepancy: design says <60s threshold for raw seconds, impl uses <120; test matches impl. No blocking issues.
