# Session Summary — 2026-04-13

**Branch:** master  
**Commit:** `feat: add expression/predicate DSL items, pipe expressions, and sigil-based parameter references`

## Changes

- Added `expression_def` and `predicate_def` as new top-level DSL items in both Lark (`ark_grammar.lark`) and pest (`ark.pest`) grammars, with `in:/out:/chain:` and `in:/check:` body structures
- Introduced pipe expression operator `|>` with `pipe_stage` rules and kebab-case function identifiers in both grammars
- Added Expressif-style sigil-based parameter references: `@var` (var_ref), `[prop.path]` (prop_ref), `#idx[n]` (idx_ref), `{nested expr}` (nested_ref) in both grammars
- Extended Rust AST (`dsl/src/lib.rs`) with `Pipe`, `ParamRef`, `PipeStage`, and `RefKind` enum variants to represent the new expression forms
- Activated ADV-001 adventure: state transitioned from `review` → `active`, permissions approved, 32 tasks created (T001–T032)
- Completed planning for 3 tasks: T001 (test strategy, 30 TCs mapped), T002 (pest grammar design, 10 rules), T005 (Lark grammar design, 6 changes), T006 (Python parser design, 3 dataclasses + 10 methods)
- Added adventure pipeline artifacts: 32 task files, test-strategy.md, 3 design documents, metrics tracking with agent token usage
