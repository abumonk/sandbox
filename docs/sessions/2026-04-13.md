# Session Summary — 2026-04-13

**Branch:** master  
**Commit:** `feat: add expression/predicate DSL items, pipe expressions, and sigil-based parameter references`

## Changes

- Added `expression_def` and `predicate_def` as new top-level DSL items in both Lark (`ark_grammar.lark`) and pest (`ark.pest`) grammars, with `in:/out:/chain:` and `in:/check:` body structures
- Introduced pipe expression operator `|>` with `pipe_stage` rules and kebab-case function identifiers in both grammars
- Added Expressif-style sigil-based parameter references: `@var` (var_ref), `[prop.path]` (prop_ref), `#idx[n]` (idx_ref), `{nested expr}` (nested_ref) in both grammars
- Extended Rust AST (`dsl/src/lib.rs`) with `Pipe`, `ParamRef`, `PipeStage`, and `RefKind` enum variants to represent the new expression forms
- Activated ADV-001 adventure: state transitioned from `review` → `active`, permissions approved, 32 tasks created (T001–T032)
- Completed planning for 3 tasks: T001 (test strategy, 30 TCs mapped), T002 (pest grammar design, 10 rules), T005 (Lark grammar design, 6 changes), T006 (Python parser design, 3 dataclasses + 10 methods)
- Added adventure pipeline artifacts: 32 task files, test-strategy.md, 3 design documents, metrics tracking with agent token usage

---

**Branch:** master  
**Commit:** `feat: add enum-aware Z3 verification and team-pipeline type declarations`

## Changes

- Created `ark/specs/pipeline/team/types.ark` with 12 top-level enum declarations (TaskStage, TeamTaskStatus, AdventureState, GitMode, CommitStyle, ReviewResult, HookEvent, HookMode, BudgetScope, ProofMethod, TCStatus)
- Patched `ark/tools/verify/ark_verify.py`: added `_collect_enum_context`, `_bind_enum_variants`, `_enum_membership_facts` helpers — enum constraints (inline or from top-level `enum_def`) are now applied as Int membership background facts in both invariant checking and BMC
- Added inductive hypothesis in `check_invariants_hold`: entity invariants are assumed in pre-state, eliminating spurious counterexamples from unconstrained initial state (fixed WorkItem and Metrics false-positives)
- Switched `Deploy.review_result` from `String` to `ReviewResult` enum; added `Reviewer.@out.verdict: ReviewResult` for typed bridge connection via `Reviewer_to_Deploy`
- Updated `Task.status` type to `TeamTaskStatus` (avoiding stdlib collision) with matching `@out` port; added `Adventure.@in.child_status: TeamTaskStatus` for `Task_to_Adventure` bridge resolution
- Added range constraints `[0..N]` to `Metrics.total_tokens/total_cost/total_runs/failures` to close underflow false-positives
- Removed Deploy class-level temporal (redundant with `#process` pre-condition); documented BMC limitation with free `@in` fields
- Pipeline verify improved from 72/81 to 84/84 passed; all 63 regression tests green
