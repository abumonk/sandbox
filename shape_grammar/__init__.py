"""shape_grammar — ShapeML-style procedural shape grammar hosted on Ark DSL.

Sibling package to `ark/`. Never imports into `ark/`. Strict dependency
direction: shape_grammar -> ark.
"""

__version__ = "0.1.0"
